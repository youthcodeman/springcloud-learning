spring:
  cloud:
    config:
      uri: http://localhost:8080
      username: user
      password: password123
      profile: dev
      label: master   # 当configserver的后端存储是Git时，默认就是master 
  application:
    name: foolbar
    
    
    