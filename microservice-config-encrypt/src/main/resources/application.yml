server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/youthcodeman/spring-cloud-learning-config
          username:
          password:

encrypt:
  key: foo  # 加密因子

  #利用curl进行加密  加密命令 :curl -X POST http://localhost:8080/encrypt -d foobar





