server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/youthcodeman/spring-cloud-learning-config
          search-paths:
            - foo   # foo路径
            - bar   # bar路径
